# TDD Practice with [sure](http://falcao.it/sure/)

## Running the Tests
From the command line, type the following to run the tests:
    ```nosetests```

You may need to ```pip install``` in your virtual environment:

* ```nose```
* ```sure```
* ```mock```
* ```six```